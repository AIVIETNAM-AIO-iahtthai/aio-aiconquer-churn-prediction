[project]
name = "aio-aiconquer-churn-prediction"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "pandas>=3.0.2",
    "python-dotenv>=1.2.2",
]

[dependency-groups]
dev = [
    "ipykernel>=7.2.0",
]
